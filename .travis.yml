language: php
php:
  - '5.5'
  - '5.6'
  - '7.0'

env:
  - GUZZLE_VERSION="5.0"
  - GUZZLE_VERSION="6.0"

install:
  - composer install
  - composer require guzzlehttp/guzzle:^${GUZZLE_VERSION}

script:
  - php vendor/bin/php-cs-fixer fix --dry-run --diff